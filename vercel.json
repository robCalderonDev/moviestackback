{
	"version": 2,
	"builds": [
		{
			"src": "index.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	],
	"env": {
		"PORT": "3000",
		"NODE_ENV": "production"
	},
	"headers": [
		{
			"source": "/api/(.*)"
			
		}
	]
		
	  
}
